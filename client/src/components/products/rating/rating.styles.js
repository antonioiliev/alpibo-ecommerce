const styles = (theme) => ({
    root: {
        width: '100%',
        display: 'flex',
        flexDirection: 'row',
        flexWrap: 'nowrap',
        alignItems: 'center'
    },
    reviewsText: {
        marginLeft: '5px'
    },
    star: {
        color: theme.palette.warning.main
    }
  });

  export default styles;
  